---
title: "Garden Journal"
navLinks: [
  { href: "/", text: "Home" },
  { href: "/new-entry", text: "New Entry" }
]
---

<header class="bg-green-600 text-white p-4">
  <h1 class="text-2xl font-bold">{{ title }}</h1>
  <nav class="mt-2">
    <ul class="flex space-x-4">
      {navLinks.map(link => (
        <li>
          <a href={link.href} class="hover:underline">{link.text}</a>
        </li>
      ))}
    </ul>
  </nav>
</header>